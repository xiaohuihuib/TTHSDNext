{
  "name": "tthsd",
  "version": "0.5.1",
  "description": "TTHSD High-Speed Downloader - TypeScript/Node.js binding (supports Node.js and Electron)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/**/*",
    "README.md"
  ],
  "keywords": ["downloader", "tthsd", "high-speed", "parallel", "electron"],
  "license": "LGPL-3.0",
  "dependencies": {
    "koffi": "^2.10.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
